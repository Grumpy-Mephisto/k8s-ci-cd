apiVersion: v1
kind: Service
metadata:
  labels:
    app: os-project
  name: os-project
spec:
  ports:
    - name: http
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: os-project
  type: NodePort
